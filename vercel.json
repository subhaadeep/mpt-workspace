{
  "framework": "nextjs",
  "buildCommand": "cd frontend && npm install --legacy-peer-deps && npm run build",
  "outputDirectory": "frontend/.next",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://mpt-workspace-production.up.railway.app/api/:path*"
    }
  ]
}
